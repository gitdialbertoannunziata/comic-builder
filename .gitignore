node_modules/
dist/
coverage/
.DS_Store

# Chiavi API e configurazione locale. Non togliere queste righe: un file .env
# committato pubblica la chiave a chiunque legga la cronologia, e revocarla
# dopo non cancella ciò che è già stato letto.
.env
.env.*
!.env.example
